vector<string> Solution::fizzBuzz(int a) {
    vector<string> s;
    for(int i=0;i<a;i++)
    {
        if((i+1)%5==0 && (i+1)%3==0)
        s.push_back("FizzBuzz");
        else if((i+1)%5==0)
        s.push_back("Buzz");
        else if((i+1)%3==0)
        s.push_back("Fizz");
        else
        s.push_back(to_string(i+1));
    }
    return s;
}
